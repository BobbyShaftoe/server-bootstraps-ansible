---

hostname_prefix: jenkins-node
hostname: hn-development.net

system_packages:
  - 'java-1.8.0'
  - 'git'
  - 'ntpdate'

artefact_src_path: 'https://s3.amazonaws.com/artefact-bucket-store'

artefact_paths:
  jenkins:
    - [ 'jenkins-node/agent.jar', '/agent' ]

script_paths:
  jenkins_agent_wrapper: '/usr/local/sbin'
  env_utility_script: '/usr/local/sbin'

users_groups:
  - [ 'jenkins', 'jenkins', '/home/jenkins' ]

users_groups_map:
  - username: jenkins
    group: jenkins
    homedir: /var/jenkins
  - username: loguser
    group: loguser
    homedir: /home/loguser


